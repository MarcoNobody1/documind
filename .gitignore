# .NET
bin/
obj/
*.user
.vs/
*.suo
TestResults/

# Node / Angular
node_modules/
dist/
.angular/

# Environment
.env
.env.local

# Secrets — real credentials live in `dotnet user-secrets`, never in the repo.
# These patterns are a safety net, not the mechanism.
appsettings.*.local.json
secrets.json
*.secrets.json
*.pem
*.pfx

# Agent tooling — generated output that resolves to absolute paths on the
# machine that produced it, so it is meaningless in a clone.
.atl/

# OS
.DS_Store
Thumbs.db
